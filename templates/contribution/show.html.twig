{% extends 'base.html.twig' %}

{% block title %}Contribution{% endblock %}

{% block body %}
    <h1>Contribution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contribution.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ contribution.description }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ contribution.montant }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ contribution.date ? contribution.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TypeContribution</th>
                <td>{{ contribution.typeContribution }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contribution_index') }}">back to list</a>

    <a href="{{ path('app_contribution_edit', {'id': contribution.id}) }}">edit</a>

    {{ include('contribution/_delete_form.html.twig') }}
{% endblock %}
