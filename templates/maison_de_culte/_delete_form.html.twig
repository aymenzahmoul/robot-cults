<form method="post" action="{{ path('app_maison_de_culte_delete', {'id': maison_de_culte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maison_de_culte.id) }}">
    <button class="btn">Delete</button>
</form>
